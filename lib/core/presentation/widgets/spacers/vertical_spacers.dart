import 'package:flutter/material.dart';

class VerticalSpacer1 extends StatelessWidget {
  const VerticalSpacer1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 1.0),
      child: SizedBox(),
    );
  }
}

class VerticalSpacer2 extends StatelessWidget {
  const VerticalSpacer2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 2.0),
      child: SizedBox(),
    );
  }
}

class VerticalSpacer4 extends StatelessWidget {
  const VerticalSpacer4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 4.0),
      child: SizedBox(),
    );
  }
}

class VerticalSpacer8 extends StatelessWidget {
  const VerticalSpacer8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 8.0),
      child: SizedBox(),
    );
  }
}

class VerticalSpacer16 extends StatelessWidget {
  const VerticalSpacer16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 16.0),
      child: SizedBox(),
    );
  }
}

class VerticalSpacer32 extends StatelessWidget {
  const VerticalSpacer32({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(horizontal: 32.0),
      child: SizedBox(),
    );
  }
}